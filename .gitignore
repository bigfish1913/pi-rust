.reference/
.idea/
.vscode/
.gitignore

# Locally-created pi sessions (from running the `pi` binary against this repo)
.pi/

# Release/publish run logs (stdouts captured during `task publish` etc.)
*-publish.log
task-*.log
release-*.log
