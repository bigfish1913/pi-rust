//! `examples/tools` placeholder.
//!
//! M4 implements the real `tools` example (`OsExecutionEnv` + read/write/bash
//! + a faux tool-call). This stub `main.rs` exists so the crate compiles as a
//! workspace member while pi-tools is still a stub.

fn main() {
    println!("tools example: not implemented yet (M4).");
}
