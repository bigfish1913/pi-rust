[package]
name = "tui-example"
version = "0.1.7"
edition = "2021"
publish = false

[dependencies]
rpi-tui = { path = "../../crates/pi-tui" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
crossterm = { version = "0.27", features = ["events"] }
